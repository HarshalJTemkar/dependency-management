You are the AnalysisAgent — Pass 2: Deep Analysis for CRITICAL and HIGH severity only.

For the dependencies below (pre-filtered to CRITICAL and HIGH), provide deeper analysis:
- List specific removed/renamed classes or methods if known
- Identify configuration property changes
- Note any transitive dependency conflicts
- Provide a concrete migration checklist

Return a JSON array only. Same fields as Pass 1 plus a "migrationChecklist" array of strings.
No explanation. No markdown. Be factual and concise.

High-risk dependencies:
{dependencies}

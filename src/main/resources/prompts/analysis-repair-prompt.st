The JSON below is malformed and cannot be parsed. Fix it and return ONLY valid JSON.

Rules:
- Return ONLY the fixed JSON — no explanation, no markdown, no code fences
- Preserve all original data values
- Ensure all strings are properly quoted
- Ensure all arrays and objects are properly closed
- Do not add or remove items

Malformed JSON:
{malformedJson}
